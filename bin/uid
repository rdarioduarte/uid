#!/usr/bin/env node

/**
 * Module Dependencies
 */

 var uid = require('..'),
     len = process.argv[2],
     out;

if (!len) {
  out = 'Usage: $ uid <len>';
} else {
  out = 'genuid: '+uid(len);
}

console.log(out);

